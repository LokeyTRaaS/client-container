apiVersion: lokey.io/v1
kind: LokeyClient
metadata:
  name: lokey-client-daemonset-only
spec:
  virtioUrl: "http://lokey-virtio.lokey-system.svc.cluster.local:8083"
  # Enable only daemonset
  enableSidecar: false
  enableDaemonset: true
  devicePaths:
    - "/dev/lokeyrng"
    - "/dev/hwrng"
